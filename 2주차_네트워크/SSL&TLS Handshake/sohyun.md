# TLS & SSL HandShake

```
SSL : CA로 부터 서버와 클라이언트의 인증하는데 주로 사용됨 
			전송 계층과 응용 계층 사이 보안 조치하는데 사용됨

TLS  : SSL 의 과거 명칭
```

https://hanjungv.github.io/2017-11-07-1_CS_SSL/

SSL 인증서 요약 

- 클라이언트와 서버 간의 통신을 제 3자가 보증화 해주는 전자화된 문서
- 통신 내용이 공격자에게 노출되고 악의적으로 변경되는 것을 막아 신뢰성 높임
- 인증서의 목표
    1. 클라이언트가 접속한 서버가 신뢰할 수 있는 서버인지 판단
    2. SSL 통신에 사용될 공개키를 클라이언트에 전달
- SSL 인증서에 들어갈 내용
    1. 서비스의 정보 (CA, 도메인)
    2. 서버 측 공개키
    

### TLS & SSL HandShake

- 클라이언트와 서버 간 통신 전 SSL인증서로 신뢰성 여부 판단하기 위해 연결하는 방식
- SSL클라이언트와 서버가 통신하는 비밀키를 설정할 수 있음
    - 그림 이해


### HandShake 에서 사용한 암호화 방식

- 서버가 "ServerHello"에서 보내는 SSL 인증서를 브라우저에 내장되어 있는 CA의 공개키로 **복호화**
- CA의 공개키로 복호화 된 SSL 인증서에는 서버의 공개키가 들어있음
- 클라이언트가 대칭키를 생성하고, 이를 SSL 인증서에서 얻은 서버의 공개키로 **암호화** 후 서버에 전송
- 서버는 자신의 개인키로 클라이언트에게 받은 대칭키를 **복호화**
- [*https://seongonion.tistory.com/76*](https://seongonion.tistory.com/76)
- https://cloudedi.tistory.com/41

### 왜 필요할까 ??

- 송신자와 수신자가 암호화된 데이터를 교환하기 위한 일련의 협상 과정
- 서버 클라이언트가 서로의 신원을 확인 가능 (서로를 인식 / 검증 )
- 세션키 생성을 협상하는 과정
